services:
  - type: web
    name: premium-outfit-hub-vton
    runtime: node
    region: frankfurt
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: FASHN_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
    plan: free
